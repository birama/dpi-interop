node_modules/
dist/
.env
.env.local
.env.production
*.log
.DS_Store
Thumbs.db
tmpclaude-*

# Test temp files
backend/tests/_*.json

# Deploy script — contient secrets JWT_SECRET et DB password, ne pas versioner
deploy-prod.sh
deploy-prod.sh.local

# Backups locaux et scripts poste (ne partent jamais dans le repo)
backups/
backend/backups/
smoke-admin.ps1
