# === PINS — Variables d'environnement production ===

# PostgreSQL
POSTGRES_USER=pins
POSTGRES_PASSWORD=ChangeMe_Strong_Password_2026!
POSTGRES_DB=questionnaire_interop

# Backend
JWT_SECRET=ChangeMe_Random_64_Chars_Here_For_Production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=https://dpi-interop.sec.gouv
RATE_LIMIT_MAX=200

# Frontend
VITE_API_URL=https://dpi-interop.sec.gouv

# App
APP_PORT=80

# SMTP (optionnel — pour les notifications email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@senum.sn
# SMTP_PASS=xxx
# SMTP_FROM=PINS <noreply@senum.sn>
